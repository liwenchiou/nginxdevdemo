# 這裡是定義上游 (Upstream) 伺服器群組的地方，也是 Nginx 負載均衡的核心。
# 這個檔案是由 docker-gen 自動生成的，請勿手動修改！
upstream api_servers {
    # 自動偵測到的容器名稱: api2
    server 172.21.0.3:3000 max_fails=1 fail_timeout=5s;
    # 自動偵測到的容器名稱: api3
    server 172.21.0.4:3000 max_fails=1 fail_timeout=5s;
    # 自動偵測到的容器名稱: api1
    server 172.21.0.5:3000 max_fails=1 fail_timeout=5s;
}
